---
server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Ahmedsebit/spring-cloudconfig-wa-tolls
          #username: uname
          #password: password
          search-paths:
          - 'station*'
          repos:
            perf:
              uri